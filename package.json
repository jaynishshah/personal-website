{
  "name": "jaynishsite",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "start": "eleventy --serve --quiet",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
